═══════════════════════════════════════════════════════════════════
🎯 GUIDE VISUEL - FIX UPDATE CONFIGURATION
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ ÉTAPE 1: SCRIPT SQL                                             │
└─────────────────────────────────────────────────────────────────┘

1. Ouvre Supabase Dashboard
   https://supabase.com/dashboard

2. Clique sur "SQL Editor" dans le menu de gauche

3. Copie le contenu de: FIX_CONFIG_UPDATE_COMPLET.sql

4. Colle dans l'éditeur SQL

5. Clique sur "Run" (ou Ctrl+Enter)

6. Tu devrais voir:
   ┌────────────────────────────────────────┐
   │ ✅ Ligne de configuration trouvée      │
   │ ✅ Toutes les anciennes policies...    │
   │ ✅ Nouvelles policies créées:          │
   │    - allow_read_config                 │
   │    - allow_update_config               │
   │ ✅ Test UPDATE réussi                  │
   │ ✅ TOUT EST PRÊT!                      │
   └────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ ÉTAPE 2: PAGE DE TEST                                           │
└─────────────────────────────────────────────────────────────────┘

1. Va sur: http://localhost:5173/dashboard/admin/test-config

2. Tu verras 6 cartes de test:

   ┌─────────────────────────────────────┐
   │ ✅ Test 1: Authentification         │
   │ Connecté: admin@kazipro.com         │
   └─────────────────────────────────────┘

   ┌─────────────────────────────────────┐
   │ ✅ Test 2: SELECT                   │
   │ 1 ligne(s) trouvée(s)               │
   └─────────────────────────────────────┘

   ┌─────────────────────────────────────┐
   │ ✅ Test 3: SELECT avec .eq()        │
   │ Ligne trouvée avec ID correct       │
   └─────────────────────────────────────┘

   ┌─────────────────────────────────────┐
   │ ✅ Test 4: UPDATE                   │
   │ Valeur mise à jour: 7.23%           │
   └─────────────────────────────────────┘

   ┌─────────────────────────────────────┐
   │ ✅ Test 5: Vérification UPDATE      │
   │ La valeur a bien été modifiée!      │
   └─────────────────────────────────────┘

   ┌─────────────────────────────────────┐
   │ ⚠️  Test 6: Policies RLS            │
   │ Test optionnel (peut échouer)       │
   └─────────────────────────────────────┘

3. En bas, tu verras le résumé:
   ┌─────────────────────────────────────┐
   │ 📊 Résumé                           │
   │ Tests réussis: 5 / 6                │
   │ Tests échoués: 1 / 6                │
   │                                     │
   │ ✅ Tous les tests sont passés!      │
   │ La configuration devrait            │
   │ fonctionner correctement.           │
   └─────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ ÉTAPE 3: VRAIE PAGE                                             │
└─────────────────────────────────────────────────────────────────┘

1. Va sur: http://localhost:5173/dashboard/admin/config-paiement

2. Ouvre la console (F12 → onglet Console)

3. Tu verras la page avec les sliders:
   ┌─────────────────────────────────────┐
   │ ⚙️  Configuration Paiement          │
   │                                     │
   │ 💰 Commissions KaziPro              │
   │ Main d'œuvre:        [====] 5%      │
   │ Matériel:            [==] 2%        │
   │ Déplacement:         [====] 5%      │
   │                                     │
   │ 🛡️  Sécurité                        │
   │ Acompte:             [======] 30%   │
   │ Solde:               70%            │
   │                                     │
   │ [💾 Enregistrer] [🔄 Annuler]       │
   └─────────────────────────────────────┘

4. Modifie une valeur (ex: Main d'œuvre à 7%)

5. Clique sur "Enregistrer"

6. Dans la console, tu verras:
   ┌────────────────────────────────────────┐
   │ Console                                │
   ├────────────────────────────────────────┤
   │ 💾 Tentative de sauvegarde...          │
   │ {                                      │
   │   commission_main_oeuvre: 7,           │
   │   commission_materiel: 2,              │
   │   ...                                  │
   │ }                                      │
   │                                        │
   │ ✅ UPDATE réussi, données retournées:  │
   │ {                                      │
   │   id: "00000000-0000-0000-...",        │
   │   commission_main_oeuvre: 7,           │
   │   updated_at: "2025-01-27T..."         │
   │ }                                      │
   │                                        │
   │ ✅ Configuration rechargée             │
   └────────────────────────────────────────┘

7. En haut à droite, tu verras le toast:
   ┌────────────────────────────────────┐
   │ ✅ Configuration enregistrée       │
   │    avec succès                     │
   └────────────────────────────────────┘

8. Recharge la page (F5)

9. La valeur doit être à 7% maintenant!

┌─────────────────────────────────────────────────────────────────┐
│ SI ÇA NE MARCHE PAS                                             │
└─────────────────────────────────────────────────────────────────┘

Scénario A: Test 1 échoue (Authentification)
→ Tu n'es pas connecté
→ Va sur /login et connecte-toi

Scénario B: Test 2 ou 3 échoue (SELECT)
→ La ligne de config n'existe pas
→ Réexécute FIX_CONFIG_UPDATE_COMPLET.sql

Scénario C: Test 4 échoue (UPDATE)
→ Problème de RLS
→ Vérifie les policies dans Supabase:
  SELECT * FROM pg_policies 
  WHERE tablename = 'configuration_paiement_globale';

Scénario D: Test 5 échoue (Vérification)
→ L'UPDATE ne persiste pas
→ Vérifie les triggers ou contraintes

Scénario E: Console montre "⚠️ Aucune ligne affectée"
→ L'ID ne correspond pas
→ Vérifie: SELECT id FROM configuration_paiement_globale;

┌─────────────────────────────────────────────────────────────────┐
│ RACCOURCIS UTILES                                               │
└─────────────────────────────────────────────────────────────────┘

Page de test:
http://localhost:5173/dashboard/admin/test-config

Page de config:
http://localhost:5173/dashboard/admin/config-paiement

Console navigateur:
F12 (ou Cmd+Option+I sur Mac)

Supabase SQL Editor:
https://supabase.com/dashboard → SQL Editor

┌─────────────────────────────────────────────────────────────────┐
│ FICHIERS IMPORTANTS                                             │
└─────────────────────────────────────────────────────────────────┘

📄 FIX_CONFIG_UPDATE_COMPLET.sql
   → Script SQL à exécuter en premier

📄 SOLUTION_COMPLETE_UPDATE.txt
   → Explication détaillée de la solution

📄 TEST_CONFIG_UPDATE.md
   → Guide de diagnostic complet

📄 PROBLEME_UPDATE_CONFIG.txt
   → Résumé du problème

📄 src/pages/dashboard/admin/TestConfigPage.tsx
   → Page de test automatique

📄 src/pages/dashboard/admin/ConfigPaiementPage.tsx
   → Page de config (avec logs ajoutés)

═══════════════════════════════════════════════════════════════════
